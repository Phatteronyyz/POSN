#include<bits/stdc++.h>
using namespace std;

typedef pair<int,int> PII;
queue<PII> pn;
int h[300100];

int main() {
    ios::sync_with_stdio(0); cin.tie(0);
    int n,l,a,b,fi,la;
    cin >> n >> l;
    for(i=1;i<=n;i++){
        cin >> a >> b;
        pn.push({a,b});
    }
    for(i=1;i<=n;i++){
        fi=
        if(h[a]==0){
            cout << "yes";
            h[a]=1;
        }
        else if(h[b])
    }

    
    return 0;
}