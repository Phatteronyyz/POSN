#include<bits/stdc++.h>
using namespace std;
char a[100100],b[100100],c[100100];
int main() {
    ios::sync_with_stdio(0);
    int n,cc=0,cb=0,i,len;
    cin >> n;
    for(i=0;i<n;i++){
        cin >> a;
        sprintf("");
        len =strlen(a)
        if(a[i]==1){
            
        } 
        if(a[i]>1){
            
        } 
    }
    sort(b,b+cb);
    sort(c,c+cc,greater<int>());
    if(cc!=0){
        for(i=0;i<cc;i++) printf("%s",c[i]);
    }
    if(cb!=0){
        for(i=0;i<cb;i++) printf("%s",b[i]);
    }

    return 0;
}